# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(act_schillingTaskLib)
ADD_LIBRARY(${ACT_SCHILLING_TASKLIB_NAME} SHARED 
    ${ACT_SCHILLING_TASKLIB_SOURCES})

add_dependencies(${ACT_SCHILLING_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ACT_SCHILLING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ACT_SCHILLING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ACT_SCHILLING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ACT_SCHILLING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ACT_SCHILLING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ACT_SCHILLING_TASKLIB_HEADERS}
    DESTINATION include/orocos/act_schilling)

